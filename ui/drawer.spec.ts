import { test, expect } from "@playwright/test";
import POManager from "../pageobjects/POManager";

test.describe("@Drawer Drawer", () => {
  test("Drawer opens and displays issue list", async ({ page }) => {});

  test("Search filters issues by title", async ({ page }) => {});

  test("Search shows no results for unknown query", async ({ page }) => {});

  test("Tab switching works — Issues, Features, Notes", async ({ page }) => {});

  test("Status filter pills filter correctly", async ({ page }) => {});

  test("Empty state shown when no issues exist", async ({ page }) => {});

  test("Pagination works with 10+ issues", async ({ page }) => {});
});
